version: '3.8'

services:
  bedrock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bedrock
    ports:
      - "8888:8888"
    networks:
      - bedrock
    environment:
      DATABASE_URL: ${MONGODB_URL}
      
networks:
  bedrock:
    external: true
 